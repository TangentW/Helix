/// Namespace for reusable Helix Hub capabilities.
///
/// The macOS application under `Hub/` is intentionally a thin frontend over
/// the discovery, onboarding, integration, pairing, and persistence APIs in
/// this module.
public enum Hub {}
