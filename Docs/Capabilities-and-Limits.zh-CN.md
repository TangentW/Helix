# 能力与限制

[English](Capabilities-and-Limits.md)

Helix 有意采用 fail-closed 策略。“Swift 编译器接受这个文件”不等于“字节码后端支持这个语法”，“编译器能为声明生成 SIL”也不等于“当前 HLBC Lowerer 与 Shell capability surface 支持它”。本文给出当前实际边界。

## 产品状态概览

| 范围 | 已实现 | 尚未认证或实现 |
| --- | --- | --- |
| Release Shell | 精确 frontend 索引、Derived Sources、Interface Archive、永久 Bridge、NativeImport 发现、Xcode 集成、bundle 泄漏审计 | 大型真实业务迁移和长期 CI 矩阵 |
| 生产 HLBC | HLBC 1.0 / HLXI 1.0 编译链、Verifier、HLVM、签名包、安全安装、不可变激活、回滚与吊销；仓库内业务 corpus | App Store 分发批准、外部 top-200 corpus、长时间 fuzz/sanitizer、真机 macro 性能与 hosted UIKit 页面 soak |
| 开发期 Live Reload | 精确构建捕获、稳定快照、body 差分、会话绑定的验证后 HLBC、认证传输、原子激活、UIKit/SwiftUI 刷新、逻辑源码映射、八代/五场景 UIKit Simulator 验收矩阵与 128 代进程内 soak | 真实 iPhone 矩阵、真机长时间 soak、交互式字节码单步调试、大型工程延迟资格 |
| Helix Hub | SwiftUI 菜单栏应用、工程发现、Hot Patch/Live Reload 事务接入、安全 helper 发现、统一 Service、精确 Build Context registry、Xcode 自动邀请与手动四位码配对 | 分发签名/公证与大范围第三方工程迁移矩阵 |
| Native 实验 | 仅显式选择的 Dynamic Replacement builder、递归/previous 测试、签名 dylib 与 loader probe | 产品支持；自动路由有意不选择它 |
| 控制面 | 客户端包与 policy 合同 | 生产 Registry、HSM 运维、审批、灰度、遥测和设备群协调服务 |

完整 SwiftPM 测试、warnings-as-errors、优化 Release 构建、iOS fixture 与仓库 Demo 分别作为证据 Gate。全部通过也不等于已经完成真实设备或分发通道认证。

## 生产 HLBC 1.0 的 Swift 子集

### 已实现

- `Bool`、有/无符号定宽整数、`Float`、`Double` 与 64 位 Apple 平台的 `CGFloat`，包括已声明的算术、位运算、比较、移位和数值转换规则。完全具体化的标量 `min`/`max` 与有符号数值 `abs` 会保留 Swift 的操作数顺序、溢出、正负零和 NaN 语义。HLBC 常量和 HLVM 值按目标位宽原样保留 bits：可表示完整 `UInt64` 值域，binary32 也不会先转成 binary64 存储，因此 infinity、正负零、NaN payload 与 signaling 状态都能穿过 canonical 编码和强类型 Bridge 往返。常用具体标量标准库 API 统一按“操作 + 现场值类型”lowering，而不是按 SDK receiver 加特例。定宽整数覆盖 `min`/`max`、`bitWidth`、`isSigned`、`magnitude`、置位/前导零/尾随零计数、`byteSwapped`、`bigEndian`/`littleEndian`、`signum()`、clamping/truncating 转换、`isMultiple(of:)`、`quotientAndRemainder(dividingBy:)`、全宽乘除和五种 reporting-overflow 运算。`Float`/`Double` 覆盖常用常量、bit-pattern 往返、exponent/significand 分解属性、分类判断、`magnitude`、`squareRoot()`、`ulp`、`nextUp`、`binade`、`significand`、`sign`、全部 rounding 规则、IEEE/截断余数、融合 `addingProduct`/`addProduct`、total ordering，以及四种感知 NaN 和正负零的 min/max；其常见 mutating 形式归一到相同的强类型操作。除零、无法表示的全宽商、有符号最小值除以负一、正负零、subnormal 与 signaling NaN 都保留 Swift 行为；对零未定义的底层 count builtin 会明确拒绝，不做猜测。
- `Bool.toggle()` 与全局 `swap` 共用一条类型驱动的值修改路径。swap 支持普通、projection、frame 或可变 closure cell 中可表示且可复制的标量、文本、Optional、Tuple 与补丁内 aggregate；重叠 storage 会被拒绝，两次读取都先于任一写入。两者都不增加 API 专属 opcode 或 NativeImport。
- `String`、`Character` 与 `Substring` 使用显式逻辑文本合同，不导入标准库私有布局。表示层 `Character` 在每个 producer 或 Shell 边界都会验证为恰好一个扩展字素簇；`Substring` 归一为 Character 序列，不保留私有 slice storage 或 index identity。已支持字面量、相等/排序、拼接、String 的 Character/String append、Substring 的 Character append，以及二者对有限 Character Sequence 的 `append(contentsOf:)`/`+=`、标量和文本插值、重复与常见 String 构造、Unicode `uppercased`/`lowercased`、count/empty、prefix/suffix/contains、String/Substring 转换、Character 序列构造、String 序列 joining，以及按扩展字素簇执行的 `removeFirst`/`removeLast`（含计数形式）、`popLast`、清空与容量提示。这些变更与 Array-backed 值共用一个可表示 `RangeReplaceableCollection` 计划，不增加文本专用字节码。直接 Character/String suffix 不会拆分已有 String，其他可表示 Character Sequence 只物化并 join 一次。可失败的标量构造另覆盖 String 到 `Bool`、String/Substring 到全部可表示有/无符号定宽整数（十进制或运行时 radix），以及 String/Substring 到 `Float`/`Double`；整数 `String(_:radix:uppercase:)` 覆盖同一个完整整数族。两条按目标类型驱动的 HLBC 操作服务全部这些组合；Verifier 会拒绝不匹配的标量/radix shape，超出 `2...36` 的 radix 会把 Swift precondition 保留成受控 trap，不引入泛型标准库 NativeImport 或逐位宽操作。String 直接执行 `count`/`isEmpty`；需要 element 的 `first`/`last`、变换、关系、`Array(sequence)`、`reversed`、基于数量的 subsequence 与 split 会经验证归一为 Character Array，再进入现有有限 Sequence cursor、adapter 与 split 状态。嵌套可表示 Character 序列也可 flatten 后重建为 String。可变大小文本操作会预扣确定性的 UTF-8 工作量与输出存储。
- 上述字面量与相等/排序能力指 String 和 Character；Substring 当前通过归一的 Collection 遍历、转换与 Shell Bridge 使用，不声称尚未实现的独立 StringProtocol 比较入口。
- Tuple、`Void` 与 `Optional`，包括 `if let`、`guard let`、`??` 和 `try?` 产生的普通控制流，也包括 Dictionary semantic SIL 产生的地址型 Optional projection。显式 `.some`/`.none` 分支与 canonical SIL 的“单个 case + `default`”穷尽式写法会归一到同一个语义 case plan；`default` edge 不会凭空生成未绑定的 payload 参数，被丢弃的线性 payload 只释放一次。Optional case 事实会沿 Tuple 与补丁内 struct 的字段路径精确追踪：已证明的 projected payload take 只消费对应字段，兄弟字段或 caller-owned storage 不能借用该证明；确定式与条件式 projected cleanup 也会保留独立初始化的兄弟字段。脱离父存储的 Optional payload address 会按物理消费者而非 opcode 拼写分类：只读 load 保留父值，消费型 load 与 `@in` 会 take，store 或 `@inout` 修改会重建嵌套 Tuple/补丁内 struct 路径后写回；同一 payload 混用破坏性消费与修改会被拒绝。frame/runtime-backed `inout` scope 可以跨 throwing same-image call，并在 normal/error 两条 continuation 上对称关闭。非 throwing 的 compiler-only `inout` 调用还可使用经过验证的临时 address storage，并拒绝重叠 projection；需要在两条 continuation 上重建写回的 throwing compiler-only projection 仍会 fail closed。声明摘要中的 `T?`、`T!`、`[T]`、`[K: V]` 与冗余分组括号会递归归一为同一套强类型表示，也适用于补丁内 stored field。
- 对可表示 payload 支持直接 Optional 强制解包与 `unsafelyUnwrapped`；nil 解包保留专用且经过验证的 trap reason。没有 payload 的运行时 `nil` 会从已验证的静态上下文获得 `T`，并统一覆盖嵌套 Tuple、collection builder、mutation/sort/split state、Dictionary key/value 与 VM equality。
- captured frontend 产生的源码级不可恢复失败会成为显式、受验证的控制流，覆盖 `precondition`、`preconditionFailure`、`fatalError`、当前构建配置下生效的 `assert`/`assertionFailure`，以及 `try!` 的错误边。当前 frontend 的静态诊断变体复用已有 trap terminator；动态 String、可表示 Error detail 与具体 `throws(Failure)` 错误的有界身份共用一条 `source_failure` terminator。消息 autoclosure 保留源码求值时机，源码位置来自 HLBC source map，也不会把 Swift runtime failure symbol 暴露为 NativeImport。
- Array 支持值语义与相等、单元素 append、`+`、`+=`、`append(contentsOf:)`、单元素与 contents 插入、`replaceSubrange`、按位置/首尾/范围删除（包括计数删除）、`removeAll(keepingCapacity:)`、`reverse()`、`removeAll(where:)`、`swapAt`、`reserveCapacity`、`first`/`last`、`firstIndex(of:)`/`lastIndex(of:)`、`min`/`max`、`elementsEqual`、`starts(with:)`、`lexicographicallyPrecedes`、`startIndex`/`endIndex`、`distance(from:to:)`、索引移动与有限偏移、`indices`、`popLast`、迭代、安全下标和返回新值的更新。单元素 append、`append(contentsOf:)` 与 `+=` 同样适用于归一的 ArraySlice destination；contents source 可以是规范化源码级 Element 身份与物理 shape 均匹配的任意受支持有限可表示 Sequence，包括 managed Array/ArraySlice/Set/Dictionary storage、String/Substring Character Sequence 与受支持的具体 progression。其余结构编辑支持所有可表示且可复制的 element 与类型匹配的 Array-backed 来源；它们统一降低到经过验证的单元素 append、半开区间替换、adapter 与通用 mutation 原语，因此边界、溢出、ownership 和分配前复制计费不依赖某个 element 或 SDK 类型。具体 `+=` metatype、包含 Tuple label 和被 HLBC 擦除差异的源码级 Element 身份、物理 shape 与线性 ownership 都会在变更前验证。容量变更提示受支持，但读取 `Array.capacity` 会明确拒绝，因为 VM 物理存储容量并不等同于 Swift Array 语义；`randomElement()` 也会在随机性及其可观察 policy 被显式表示前 fail closed。符合类型约束的 Dictionary 支持构建、相等、查找、下标赋值、惰性的 `subscript(_:default:)` 查找与 scoped 修改、`updateValue(_:forKey:)`、`removeValue(forKey:)`、`removeAll(keepingCapacity:)`、`keys`/`values` 序列物化、`init(uniqueKeysWithValues:)`、`reserveCapacity` 与迭代。插入、替换和删除共用一个强类型 Optional-update 原语，同时返回旧值和更新后的 Dictionary；key/value view 共用一个按所选 element 类型参数化的投影原语。默认查找只在缺键时调用 autoclosure；它的 `_modify` 与 Array element `_modify` 共用 scoped frame storage，并在 `end_apply`、`abort_apply` 两条出口都回写，因此嵌套和可抛错 inout 修改不需要集合 API 专用字节码。两条路径都会在复制前预扣遍历工作量和输出存储。可表示 element/key/value 还支持泛型 `Array()`/`Dictionary()` 空构造。Array、Dictionary、Set 共用直接的 `count`、`isEmpty`、`first` 查询，Array 另通过已表示的双向存储支持 `last`；已经归一的 Array-backed view 使用同一套查询。相等语义统一递归覆盖 Bool、定宽整数、浮点、String，以及受支持的 Optional、Array、Dictionary 与 Set；Dictionary/Set 按无序值比较，集合相等保留 Swift 的共享存储快速路径，Float/Double 则保留 Swift 的 NaN 与正负零行为。自然排序仍只允许 VM 无需执行用户 witness 就能比较的整数、浮点与 String element；非可变 `sorted()` 可接受 element 满足该约束的任意可表示 managed Collection 或受支持有限 progression，而可变 `sort()` 支持具有可表示整数索引的 Array-backed mutable Collection。Comparator 驱动的 `sorted(by:)` 通过普通已验证 closure ABI 支持任意可表示且可复制的 Array、Set、Dictionary element 与受支持有限 progression，可变 `sort(by:)` 使用同一 Array-backed 整数索引边界。Set 支持空值、字面量、Array/Set/受支持有限 Sequence 构造，`count`、`isEmpty`、`first`、`contains`、`insert`、`update`、`remove`、`popFirst`、`removeFirst`、`removeAll`、容量提示、迭代，union/intersection/subtraction/symmetric-difference 系列，以及常见 equality/subset/superset/disjoint 关系。Set 的顺序不会参与相等性；HLVM 只在同一个值内保留确定的迭代顺序，以保证执行和诊断可复现。Dictionary key 与 Set element 的 Hashable domain 使用同一个递归 VM 值类型族。下载代码不能执行任意用户 hashing/equality，因此自定义 `Hashable` 或 equality witness 会 fail closed。完全具体的 `map`、`flatMap`、`compactMap`、`reduce`、`reduce(into:_:)`、`forEach`、`first(where:)`、`contains(where:)`、`allSatisfy`、`count(where:)` 与 comparator 驱动的 `min(by:)`/`max(by:)` 会在可表示的 String、Array、Dictionary、Set 上复用同一套已验证 closure 遍历；Dictionary element 使用原生 `(key: Key, value: Value)` 元组形状。String 与三种 stored container 都支持保留逻辑容器类型的 `filter`，Dictionary 另支持 `mapValues` 与 `compactMapValues`，其专用 key/value callback ABI 从同一元组遍历投影。自然 `min()`/`max()`、等值 `contains(_:)` 与 `elementsEqual`/`starts(with:)`/`lexicographicallyPrecedes` 也接受满足对应 VM-defined Comparable/Equatable 约束的 managed Collection 与受支持有限 progression；关系两端只要 element 形状一致，就可以使用不同来源类型。这些只消费 element 的操作直接流式遍历同一个 cursor，不物化中间 Array。反向 `last(where:)` 支持 String 与 Array-backed 来源；具有可表示整数索引的 Array-backed 来源会为 `firstIndex(where:)`/`lastIndex(where:)`、可变排序、`reverse()`、`removeAll(where:)` 和 `partition(by:)` 保留逻辑基址。String 与 Array-backed 来源支持 `prefix(while:)` 和 Collection `drop(while:)`。产生新结果的变体共用 invocation-local 线性元素缓冲，并按结果类型收尾为 String、Array、Dictionary 或 Set，避免反复 copy-on-write 编辑。其中 `last` 搜索会从尾部调用 predicate，comparator selection 则保留 Swift 的参数顺序和“tie 取最早元素”行为。Comparator 排序使用有界且稳定的归并状态机。partition 保留 Swift 的 low/high 双向 predicate 顺序与原地交换排列；`removeAll(where:)` 按源顺序访问，使用同一套通用可变快照做半稳定压缩，再一次性删除后缀。可变排序只在所有 comparator 成功后写回；partition 与 predicate 删除则会在 predicate 抛错时写回此前已经完成的交换，callback 已产生的其他副作用同样可见。Array-backed Collection 与 String 的 `split` 同时支持 `separator:maxSplits:omittingEmptySubsequences:`（element 具备 VM 递归定义的 Equatable 语义）和可抛错的 `whereSeparator:`（任意可表示且可复制的 element）。两者共用带 kind 校验的线性 range 状态：被省略的空段不消耗 `maxSplits`，达到上限后不再调用 predicate，负数上限会在任何 callback 前 trap，抛错边会销毁全部临时 ownership。返回的 subsequence 保留元素顺序；Array-backed 整数索引来源还会保留每个片段的逻辑下界。String subsequence 使用表示层 Substring Character Array，不保留 `String.Index`。具体来源可归一为 Array-backed 序列时，也支持 Sequence `prefix(while:)`；惰性的 Sequence `drop(while:)` 仍会被拒绝，因为立即物化会改变 predicate 副作用的发生时机。`enumerated()`、`Array(sequence)`、异构 `zip` 与 `reversed()` 通过同一受验证物化路径接受 String、managed Array、Set、Dictionary 与有限 progression 来源；String 与 Array-backed adapter 另外支持基于数量的 `dropFirst`/`dropLast`/`prefix`/`suffix`、整数 index prefix/suffix、`Range<Int>`/`ClosedRange<Int>` 与单侧范围 slice、`joined()`、`joined(separator:)`、迭代，以及 base 已经可归一时组合出的 `Slice<Base>`。`repeatElement` 与 `Array(repeating:count:)` 继续复用同一 Array builder。嵌套可表示 Character 序列可以 flatten 并重建为 String。`ArraySlice` 与递归 Array-backed 的 `Slice` 会在嵌套 view、边界、移动、距离、`indices`、元素/范围下标、搜索、predicate subsequence、split、排序和受支持变异之间携带逻辑基址；`Array(sequence)` 会有意把新 Array 重置为零。具有不透明私有索引的 Collection 仍会 fail closed，不会被近似成整数 offset。payload 可表示为补丁内局部值时，`Optional.map`/`flatMap` 与具体 `Result.map`/`mapError`/`flatMap`/`flatMapError` 统一使用带显式 payload ownership 的选定 case 变换；`Result.get()` 会把 success/failure 投影到经过验证的 normal/error 边。局部 `Result` 当前不能嵌入 native handle。
- 可表示的整数索引 Collection 还统一支持变异式 `formIndex(after:)`、`formIndex(before:)`、`formIndex(_:offsetBy:)` 与受限偏移；失败的受限变体把 index 停在 limit 并返回 `false`，溢出、越界和 logical base 均保留 Swift 语义。Repeated 与 Array 共用零基 Array-backed 模型，包括 start/end、移动、距离、indices、安全下标与泛型 associated-index 的间接结果 ABI；ArraySlice 与递归 Array-backed Slice 继续保留逻辑基址。Dictionary/Set 的 `minimumCapacity` 构造与空构造共用强类型计划，容量本身在当前表示层不可观察，但负数仍在任何构造前以 Swift precondition trap。Array、Dictionary、Set 与已归一 Collection 的直接 `underestimatedCount` 返回精确值；Zip2Sequence 会递归保留来源的 Sequence-witness 估算规则，遇到 enumerated 或 flattened/joined 来源时返回 `0`，不能用已经物化结果的精确长度替代。
- `Result(catching:)` 只调用一次同步 throwing closure，并由受验证的 normal/error continuation 构造与其他具体 `Result` API 相同的局部 enum；它不通过 NativeImport 调用 Swift 泛型实现，也不增加 API 专属 opcode。本地名义聚合可承载受管的 `Error` existential；具体局部负载仍受 capability 约束，并在 existential 构造时和 VM 边界执行深度与 fuel 检查。局部 `Result` 仍不能嵌入 native handle。
- String 的 element consumer 会先经过一次受验证的 Character Array 物化，再进入共享 cursor；无需中间 Array 的直接 streaming 适用于已经由 managed Collection storage 或 progression cursor 表示的来源。
- 泛型间接结果可通过与普通 store 共用的强类型 compiler-address sink，初始化原始 Array 字面量 storage 的整个 element 或其中一个 Tuple 字段。
- `Sequence.prefix(while:)` 的直接 Array 结果形式还接受所有可表示的有限 specialization，包括受支持的 progression；它不要求来源能归一为 Array-backed Collection。惰性 `Sequence.drop(while:)` 仍会被拒绝，因为立即物化会改变 predicate 副作用的发生时机。
- Dictionary 还支持 `init(_:uniquingKeysWith:)`、Dictionary 与可表示 Sequence 两种 `merging`/`merge`，以及可表示 managed Array/Set/Dictionary 来源和已归一 Array-backed adapter 上的 `init(grouping:by:)`。grouping 通过融合后的 Array-valued 累积 append 线性增长 bucket，不会反复复制整组。combine callback 只在重复 key 上调用；首个等价 key 的 identity、插入位置、来源遍历顺序和 `rethrows` 都会保留。可变 `merge` 抛错时保留此前已成功的前缀修改，返回新值的构造则销毁部分结果。
- frontend 生成的 `_arrayForceCast` 与 `_dictionaryUpCast` 只有在原始类型仅有 Tuple label 差异、且递归归一后的完整来源/目标 VM 类型也完全相同时才会被接受，用于覆盖直接或嵌套容器中的 label 擦除。仅有相同底层表示并不充分；真正改变 element、key、value 或 reference type 的转换仍会被拒绝。
- 结构化分支、循环、switch、调用、递归、显式业务错误边和带 payload 的局部 Error 值。真实 frontend 语料已覆盖三元、`if`、`switch` 表达式，多 Optional binding，`repeat-while`、`for where`、带标签的 `break`/`continue`、Tuple 与 Optional 模式匹配、`if case`、`guard case`、`for case`、`while let`、`while case`、`fallthrough`、提前返回，以及循环与返回清理路径上的 `defer`。直接调用的局部函数、带捕获的 closure 值、作为 transform 传入的局部函数，以及具体 operator function reference 也统一走普通的受验证函数与 closure 路径。
- 所有已支持有/无符号定宽整数的 `Range`/`ClosedRange` `for` 循环，以及这些整数、`Float`、`Double` 和 64 位 `CGFloat` 的 `stride(from:to:by:)`、`stride(from:through:by:)`。`Range.contains`/`ClosedRange.contains` 还支持已支持整数、浮点和 String 边界。Lowerer 统一生成以 Optional 为 cursor 的强类型 HLBC progression，不依赖标准库 Iterator ABI；下面列出的有限具体 Sequence 操作会复用这个 cursor，不增加 API 专属 opcode。零步长、非法区间边界保留 Swift trap，整数极值也不使用会碰撞的 sentinel。
- 有限具体整数 `Range`/`ClosedRange` 与受支持数值 stride 会复用 managed Collection 的正向高阶遍历，覆盖 `map`、`flatMap`、`filter`、`compactMap`、`reduce`、`reduce(into:_:)`、`forEach`、`first(where:)`、`contains(where:)`、`allSatisfy`、`count(where:)` 以及 comparator 驱动的 `min(by:)`/`max(by:)`；产生新容器的变换返回 Array，并保留相同的短路、抛错和可变捕获语义。有限整数 Range Collection 还支持反向 `last(where:)`；`Range<Int>` 的 `firstIndex(where:)`/`lastIndex(where:)` 直接返回匹配 element 这一精确 index，而 `ClosedRange.Index` 保持不透明。反向路径通过已有的有界强类型 adapter 物化，保留 predicate 顺序和短路。它们还支持等值 `contains(_:)`、自然 `min()`/`max()`、与 managed Collection/另一个 progression 混合的 Sequence 关系、自然/comparator `sorted`、`Set(sequence)`，以及 sequence operand 已完全具体且有限的泛型 Set 代数。整数 Range/ClosedRange 的 `count`、`isEmpty`、`first`、`last` 是直接读取 bounds 的常数时间查询；count 覆盖 element 的完整位宽，并在基数超过 `Int.max` 时 trap。可表示 Comparable bounds 的 Range 也支持 `isEmpty`，但不会因此获得迭代能力。只消费 element 的操作保持短路或单 candidate 流式执行；排序与 Set 结果才通过统一强类型 builder 物化。`Array(sequence)`、`enumerated()`、`reversed()` 与异构 `zip` 同样只在结果需要存储或随机访问时物化 progression。受支持整数 Range 之外的 progression 反向 predicate/返回 index 搜索、其他 index-sensitive Collection boundary/subsequence，以及把单侧 partial range 当作可能无限的 Sequence 来源，会在方向、index identity、复杂度或终止条件能够精确表示前继续 fail closed。
- 整数 Range/ClosedRange 的 `underestimatedCount` 与 `count` 共用精确 bounds 计算；StrideTo/StrideThrough 则通过同一个受 fuel 限制的 cursor 做精确 Sequence-witness 计数，只使用常数额外空间。半开定宽整数 Range 的计数式 `dropFirst`/`dropLast`/`prefix`/`suffix` 会在常数时间内移动并 clamp 一个强类型 bound，不把完整基数收窄到 Int，覆盖 Int/UInt 64 位极值；ClosedRange 的不透明 SubSequence index 仍不据此放宽。
- 可表示 Comparable bounds 的 `Range` 还支持 `overlaps`、`clamped(to:)` 与上下界访问；空区间永不 overlap，clamp 在相等时保留原始 bound，包括浮点 signed zero。它们与 `isEmpty`、`contains` 共用强类型 compare/select 与 progression storage 生命周期，不引入泛型 NativeImport 或逐 API opcode。
- `PartialRangeFrom`、`PartialRangeUpTo`、`PartialRangeThrough` 的 containment（包括 switch pattern 产生的 `RangeExpression.~=`）对可表示整数、浮点、String 与 Character bounds 共用一套强类型比较计划；动态无序浮点 bound 仍保留 Swift 构造 trap。具有可表示整数索引的 Array-backed Collection 以 `Range<Int>`、`ClosedRange<Int>` 或单侧 `Int` bound 下标时复用既有 range/suffix/prefix subsequence 操作并保留下界；全范围 `[...]` marker 则在可表示 Array-backed 或 String/Substring 来源通过既有物化边界归一后被消除。这些范围值只存在于 Compiler：不会序列化 Swift 泛型集合 ABI，不会把标准库方法注册为 NativeImport，也不会新增逐源码 API opcode。自定义 `Comparable` witness 与 `String.Index` 等私有 index identity 仍会 fail closed。
- 可在现有受监视文件中新加、且不导出到原生 ABI 的文件或 module scope 补丁内非递归 stored struct/enum；支持具体 `Result`、字段读取、enum switch、实例/静态计算 getter/setter 与受支持的 mutating helper。逗号分隔的 enum case（包括 labeled/unlabeled associated value 混合）按顶层边界解析，重复、空或畸形 case 摘要会 fail closed。嵌套声明保留完整 namespace identity。它们是仅属于当前 generation 的 VM 值，不是新加载的 Swift metadata。
- 当前 module 中已有的 Shell struct/enum 可以通过冻结的逻辑值 codec 穿过 eligible Entry。当前 profile 要求类型 copyable、非泛型、非递归且可由源码安全重建；支持普通、`borrowing`、`consuming`、`mutating` 实例方法（包括 extension method）、全局参数/结果、normal/throwing 路径、嵌套声明、private stored field、符合 `Error` 的 enum，以及递归可表示的标量、文本、`Any`、Optional、Array、Dictionary、Set、Tuple 和其他 eligible 本地冻结值。Interface 与 device hash 会冻结 struct field 和 enum case、label、顺序、精确源码类型拼写、copyability 及受支持的 conformance 事实。生成的同源构造 hook 与流式 Bridge codec 在不读取 Swift ABI layout、不使用反射或运行时 metadata 的前提下重建逻辑值；Patch 编译会重放精确源码 shape，Verifier 还会独立要求 archive 定义完全一致。每个 Entry 至多允许一个同步逻辑 `inout` 区域；存在该区域时，normal 与已声明 error continuation 返回唯一且类型精确的 writeback，trap 不返回写回，多个或 async 区域仍拒绝。
- 已有同步计算属性与下标会按一个父替换声明分组，并为每个 accessor 建立精确 root。支持简写/显式 getter、get/set 对、自定义 setter value 名、`mutating get`、`nonmutating set`、global/实例/static/class 属性、实例/static 下标、源码 extension，以及 `private(set)` 等 accessor 独立可见性。可变冻结值 receiver 使用唯一的逻辑 `inout` 区域，在 normal 与已声明 error 出口（包括普通 `throws` getter）写回；未选择的兄弟 accessor 由生成 fallback 保持上一实现。泛型 accessor 声明、位于泛型 nominal/extension 上下文中的 accessor，以及生成的文件作用域代码无法命名 private 嵌套 receiver 的 accessor 仍会 fail closed。
- 新增普通函数、private 方法和计算属性会作为同一 image 的普通函数、getter 或 setter 被传递发现并编译，不要求它们预先出现在 Shell EntryIndex 中。patch-local `final class` 具有 HLVM 自己的引用 identity、字段 storage 和方法调用；纯 HLVM class 仍不能跨原生边界。
- 新增 `final` class 可以选择一个 HLXI 已冻结、`NSObject` 兼容的 reference superclass。Runtime 为每个不可变 image 注册 Objective-C host，使对象能以该 superclass（包括 `UIViewController` 或项目基类）的身份交给原生代码。当前 hosted profile 仅支持继承的无参初始化、无新增 stored property，以及无参或单个 `Bool` 参数的 `Void` override；原生侧不能识别补丁新增的 Swift 具体类型。
- 同步补丁内 `inout` 与 `mutating` helper，并受 Address、access、alias、ownership、同 frame/同 block 规则验证；其中包括编译器为捕获可变局部变量的 `defer` helper 生成的 `@inout_aliasable` / `@closureCapture $*T` 物理 convention。
- 捕获 copyable 可表示值的同步补丁内 closure，包括 nonthrowing 与 throwing 调用路径。具体 `throws(Failure)` 函数与 closure 会在 nonescaping 调用、escaping storage、aggregate、具体泛型转发及错误 continuation 之间保留精确的、符合 `Error` 的补丁内 nominal；该原始 typed channel 由 `typed-throws-1` 门禁。到 `throws(any Error)` 的转换必须由编译器已经具体化的 Swift reabstraction thunk 表示，VM 不会自行推断错误类型协变。`throws(Never)` 会归一为 nonthrowing，不可能到达的 `Never` normal continuation 也没有寄存器 payload。可变局部值统一提升为与具体类型无关的 VM cell，可覆盖标量、String、Optional、Array、Dictionary、Set、Tuple、补丁内 struct、字段投影、嵌套捕获，以及 Swift 为 escaping closure 生成的 `{ var T }` box。字段敏感的“确定/可能初始化”分析还覆盖分支初始化、条件覆盖与清理，但不会把可能初始化的值当成可读值。它包括同 image helper 的 `@escaping` 参数、从同 image 函数把 closure 返回给调用者，以及 closure 再捕获另一个 closure。closure 还可作为 Optional、Tuple、Array、Dictionary、补丁内 struct/enum/class 字段、可变 closure 变量和高阶参数/返回值；capture list、递归 closure 变量、局部/绑定 method 引用、补丁内 enum case 构造器、具体的 `Optional.some`/`Result.success` 构造器、补丁内 struct initializer、补丁内 static factory、多 trailing closure 与 escaping autoclosure 都复用同一值模型。具体 closure ABI 会保留每个参数的 owned/borrowed/inout convention，包括受支持高阶操作中的 `@in_guaranteed` Optional、imported SDK reference value、普通 same-image closure 的 normal/throwing inout 调用，以及 `reduce(into:_:)` 使用的 scoped frame-owned mutation。可复制的线性捕获（例如 imported reference）会在构造 managed closure context 时复制；每次调用复用 borrowed 目标参数，并为 owned 目标参数生成受资源预算约束的新副本，因此同一 closure 可以安全地重复调用。inout 线性捕获仍会被拒绝。完全具体的 direct/indirect-result reabstraction thunk 会作为 image-local compiler-generated function 链接，只有被 partial apply 时才使用 closure-body role；仅被直接调用的 closure 与 `defer` helper 会保留物理 capture ABI，并作为 concrete specialization 链接，不会走 NativeImport。上述 Swift callable 携带的具体 nominal metatype 会以物理参数位置和 nominal/value identity 作为编译期事实校验，并在 direct/partial application 进入 HLBC 前擦除。自定义 struct initializer 可以通过字段 projection 构建补丁内 struct；Compiler 只会在全部必要字段初始化后，才使用与 Tuple 初始化相同的字段路径和 ownership 模型重建 aggregate。on-stack `partial_apply` 与 `withoutActuallyEscaping` 使用显式动态 scope identity：Verifier 证明 normal/throwing 的每条 CFG 路径都会关闭 scope，VM 会拒绝 scope 结束时仍存在于显式 storage，或仍可经后续语义用途到达的 closure；已经无后续用途的 SSA 物理别名不会被误报为逃逸。词法期 nonescaping closure 可以通过 managed-cell capture ABI 借用调用者的 `inout` address；address access 结束即使借用失效，静态验证与 VM 都要求先关闭 closure，escaping capture 则拒绝。静态 scope provenance 会跨 block 参数与含 closure 的 aggregate 传播；创建 escaping callback 时还会对嵌套 closure、集合、mutable cell、补丁内对象与仍存活的本地 weak/unowned referent 执行受 budget 限制且 cycle-safe 的完整值图扫描，因此引用型 storage 也不能隐藏 lexical scope。其他普通 closure 值必须留在同一次固定 generation 的 HLVM invocation 内；只有下条精确 NativeImport callback 参数可以通过受管 handle 延长生命周期。返回与嵌套捕获由 `escaping-closure-values-1` 门禁，managed cell 由 `mutable-captures-1` 门禁。Compiler 生成且不再包含 archetype、metadata 或 witness 依赖的完全具体 specialization 可以直接使用；语义 SIL 中在同 image 的具体 `apply`、`try_apply` 或 `partial_apply` 调用点使用的源码泛型 helper 也会被单态化。连续 generic clause 中的 protocol/composition、same-type、`AnyObject`/superclass 与 dependent associated-type requirement 必须由精确完整的 frontend witness record 具体证明；条件 conformance 只有在实例化 requirement 可于同一闭合环境递归证明时才可用。不同参数列表获得确定性的 image identity，受约束 extension method、递归、rethrows、高阶与 escaping function-value 形态共用同一路径。文件/module scope 泛型 struct、enum 与 final class 只会为可达参数物化具体字段、case、initializer 和 method；frontend entry result buffer 还能在不引入运行时 opaque metadata 的前提下具体化单个、依赖外层泛型及保持顺序的多个 opaque result。未解析参数、pack、无法证明的条件证据，以及仍需运行时 metadata 或开放式 witness dispatch 的 body 会明确失败。
- 当前 module 内完整、非条件式 conformance 的闭合不可变 protocol existential。源码可使用局部 `any P`、protocol composition 与继承 requirement、struct 和补丁内 class conformer、`AnyObject` 约束、不可变 opening/erasure、闭合 narrowing/widening、Array 存储、绑定 method、closure/直接函数返回、同步 throwing requirement、具体 cast，以及 protocol existential 之间的 `as?`/`as!`。Compiler 不把 protocol identity 写入 HLBC，只生成精确的表示类型集合和有限的“表示类型 → function”表；Verifier 要求类型唯一且已知、target 为 concrete specialization、非 receiver ABI/effect 一致，并证明 receiver ownership 安全，每张表或 cast 集合最多 4,096 项。HLVM 只做精确匹配，并按完整线性查找规模计入 fuel。这些值只在 image 内有效：protocol existential Shell root 会被判为 ineligible，携带 Swift protocol value 穿过 Shell 或普通 NativeImport 的调用会在生成字节码前拒绝。另行证明的 Objective-C `!foreign` protocol 擦除仍走冻结的原生 `AnyObject` 路径。条件式、retroactive/imported conformance、开放 conformer 集合，以及 mutable existential opening/writeback 继续 fail closed。
- 未改动的 eligible Swift callable，以及已声明且表示保持的 NativeImport callable，也可以直接成为 closure 值，而不把 archive 中的旧实现复制进补丁，也不增加 API 专属 VM adapter。它统一覆盖无捕获的全局/自由函数、把 native receiver 复制进普通 closure context 的绑定实例方法，以及校验并擦除 compiler-only metatype 的 initializer。统一的 `make_closure` 会冻结 image function、Shell `EntryIndex` 或 `NativeImportID`；该 callable ABI 必须完整且表示保持，调用点默认参数投影与仅适用于直接调用的 adapter 会被拒绝。调用参数对应 Shell ABI 前缀，`partial_apply` 捕获对应后缀；所有权、结果、callable effect 和边界错误检查与 image-local closure 共用同一模型。只要被引用入口含 borrowed 参数，直接、throwing 与 closure target 调用都必须显式声明同一个 `borrow-calls-1`。普通与 throwing 调用都经当前调用固定的 generation 路由；同一 target 因而可以传入声明为 nonescaping 或 escaping 的 NativeImport callback，已经被原生侧保留的 callback 在激活或回滚之后仍会调用创建它的固定代际中的原始入口。补丁内 typed error 不允许借此穿过 Shell 边界。
- `withExtendedLifetime` 会降低为类型通用的同步 closure scope：一份可表示的 lifetime anchor 副本会跨无参 body 的 normal 与 typed-error continuation 保持存活，并在两条出口释放，不会调用标准库的泛型运行时 ABI。
- Swift frontend 已提供具体错误 substitution 的标准库高阶专门化同样保留 `Failure`；旧式 `rethrows` 只有在具体 thunk 擦除后才进入 `any Error`。
- 经过上下文定型的运算符/重载函数引用、unbound instance method、同步 `@MainActor` closure、lazy/可变/条件 closure 局部变量，以及 frontend 已折叠的纯文件级/静态 closure 常量也进入同一 callable 图。同一个递归局部 helper 可以同时直接调用和转换为 closure，而不会产生互相竞争的 image identity 或 capture ABI。
- 安全的 `weak` 与 checked `unowned` capture list，以及被 closure 捕获的 weak 局部变量，会复用同一 managed-capture ABI。共享的不持有对象 handle 可以指向补丁内 class 或已冻结 identity 的 native reference：对象释放后 weak load 变为 `nil`，已失效的 checked-unowned load 则触发受控 VM trap。该 storage 及其创建、访问指令统一由 `non-owning-references-1` 门禁。
- NativeImport 的 callable 边界穿越走一套精确且不依赖 framework 的生成式 bridge profile。typed AST 提供源码 closure 类型，canonical SIL 提供物理 `@noescape`/escaping 生命周期、Objective-C block reabstraction、ownership 与 global-actor 证据。MainActor provenance 会穿过 frontend conversion、Optional 包装与不可变的推断型局部别名继续传播；源码显式书写的 function type（包括有意的 actor 擦除）始终优先。当前接受直接或 Optional 的同步、nonthrowing callback；callback 参数可递归使用普通 native bridge value 家族，包括以有界 opaque proxy 表示的直接或 Optional `Error` existential，但不能是 `inout` 或 image-local value。如果生成的 Swift 调用能够证明嵌套 callable 是 escaping，还可接收一层直接或 Optional 的原生 callable；其自身签名必须同步、nonthrowing 且不再含 closure，使用 callback bridge value（包括受限 `Error` 代理）。VM 调用它时会保留原生 identity、精确 ownership 与 MainActor 要求、资源计量，并拒绝同一非 Sendable callable 的重叠执行。NativeImport 本身也可以按同一签名 profile 返回直接或 Optional 的原生 callable；返回值天然 escaping，必须由生成 Bridge 创建具有 identity 的 target，并在同步 import context 关闭前按精确 deadline 与已签名资源上限编码，image-local closure 不可冒充。其参数和结果不能出现第二层 callable，callable 容器仍会拒绝。callback 结果可以是 `Void`，也可以是具有确定性失败值的普通递归 bridge value：标量、文本、`Any`、Optional、空集合及元素都可默认构造的 Tuple 均可使用。直接 native 结果因没有 framework-neutral 的失败实例而拒绝，Optional native 结果则可用 `nil`。边界只携带有界的动态类型文本名，不携带原生 payload、类型 metadata 或语义错误 identity；Shell entry、普通 NativeImport 参数/结果以及 callback 结果会拒绝 `Error`。nonescaping callback 只在当前原生调用期间有效；escaping callback 可以被该精确参数保留、超过原 VM invocation 的生命周期，并携带 generation lease 在之后重新进入原 image。在通用 Swift `Sendable` 语义实现前，同一 Runtime Engine 会串行化所有 callback：允许同线程递归，进行中的 NativeImport 不能把 callback 跳到另一线程，不同线程的重叠 callback 会 fail closed。UIKit animation/transition/property-animator callback、`DispatchQueue.async`/`asyncAfter`、`DispatchGroup.notify`、`OperationQueue.addOperation`、`Timer.scheduledTimer`、`URLSession.dataTask`、`NotificationCenter.addObserver`、`NSPredicate`、`FileManager` enumeration 与 `UIContextualAction` completion 都由这条通路覆盖，而不是逐 framework 适配。callback 执行或结果解码失败时，wrapper 会先返回确定性的 ABI 值；活动 importer 会保留错误并在 native frame 返回后 trap，已经脱离原调用的 escaping invocation 则上报 Runtime telemetry。callback 旁被省略的源码默认参数会记录为经过检查的物理到逻辑投影，只有 SIL provenance 与 ownership 验证通过后才由生成的 Swift 调用按语言规则补上。`convert_closure` 只允许把其余 ABI 完全一致的非隔离 closure 收紧为 MainActor；Verifier 拒绝反向擦除、ownership/result/effect 改写，以及 converted lexical closure 进入 escaping NativeImport，VM 则保留收紧后的逻辑 signature 与复制的 capture context。仅作为 `destroy_not_escaped_closure` 操作数的 canonical-SIL `Optional.some` 会被视为编译器作用域载体；一旦还有其他语义用途就 fail closed。编译器生成的 bound-method factory 只有在所有返回值都能证明指向 MainActor body 时，才恢复被 canonical SIL 擦除的嵌套 actor；普通源码 factory 不会被推断或改写。
- 省略 Optional Objective-C block 默认参数时，编译器生成的 `Optional.none` 只有在与冻结物理 block 拼写完全一致时才会无 payload 投影，不会物化 VM closure。
- 完全具体的 patch-local image function 之间支持顺序 `await`，包括多个 await、`async throws` 的传播或捕获，以及精确的 nonisolated/MainActor 切换。Compiler 只接纳静态绑定的 `@async` `apply`/`try_apply`；Verifier 会拒绝跨 suspension 仍存活的 address、access 或借用 address 的 storage。精确的 async Shell 函数与方法 root 使用绑定 hash 的永久源码 body wrapper：它在第一次 suspension 前选择词法 original，或固定不可变 generation 与已编码参数；挂起后的安全 fallback 只通过 async OriginalCatalog。每个精确源码 body 上限为 64 KiB；超限声明仍会完成索引并被判为 ineligible，而不会中止整个 module 索引。若 private 嵌套 receiver 无法由同源码的 original thunk 在文件作用域命名，对应 async instance root 也会拒绝。生成式 suspending NativeImport 覆盖完全具体、直接 ABI 的 nonisolated/MainActor 全局/静态/实例函数与 getter，并支持普通 `throws`；显式 async catalog factory 还可暴露预先声明的 initializer，但不会自动发现 async initializer。async computed accessor 暂时只能作为 NativeImport，不能成为 Shell root；async closure 传输与 completion-handler 自动转 async 仍拒绝。`sourceScope` 的访问 profile 使用 `pure`、`read`、`read-write`。禁止主线程执行时 `maximumBoundedDurationMicroseconds` 范围为 1...2,000；允许主线程时范围为 1...16,000，但只有精确 MainActor 声明可使用超过 2,000 微秒的部分，同一 scope 的其他同步声明仍会被钳制到 2 ms。`maximumSuspendingDurationMicroseconds` 范围为 1...60,000,000。
- VM-owned `Any`、`is`、`as?`、`as!`。闭合的递归逻辑类型描述符会区分有意共用 HLBC storage 的源码类型，包括 `Int`/`Int64`、`UInt`/`UInt64`、`Double`/`CGFloat`、String/Character、Substring/Array 以及 Array/ArraySlice。Optional、Array、Dictionary、Set 与 Tuple 的递归转换会保留这些身份；ArraySlice 与补丁内 nominal value 支持精确动态身份。递归 Dictionary/Set 转换产生的 key/element 碰撞会以受控 VM trap 保留 Swift 的终止行为。v1 描述符最多允许 32 层嵌套 wrapper、64 个 tuple element，以及每个 tuple label 256 个 UTF-8 字节。Swift existential metadata、native object 和线性生命周期不会进入下载字节码。
- 完全具体的默认参数 generator。生产与开发编译器会把可达 `fA...` thunk 一起链接并纳入传递实现指纹；当前只承诺一个完整 module source set 内的 eligible 调用点。跨 module 的 public/package 默认值、非 eligible 调用点或仍需泛型 metadata 时要求完整构建。
- 所有新 Shell 会自动冻结原生 Swift 文本渲染能力：普通 `Swift.print`、`Swift.debugPrint`、`String(describing:)` 与 `String(reflecting:)`。两种 print 保留 variadic separator/terminator 语义；两个泛型 String initializer 会在 Compiler 证明源码动态类型可精确重建后，经固定的 `Any -> String` 适配层调用。共享 codec 支持递归的标量、文本、Optional、Array、Dictionary 与 Set，所有操作统一限制 64 KiB 输出；无需 App 手工配置 Catalog，也不引入泛型 metadata、witness table、新 opcode 或合同版本。
- 受管 Debug 会测量所有“已经冻结 imported native type”所属 module 的公开成员。捕获 toolchain 的 symbol graph 负责提名满足 minimum OS 且未 deprecated 的 API，同一 typed AST/canonical SIL 流程只冻结唯一且 Bridge-compatible 的同步 initializer、实例/静态 method 与可读/可写 property。完整 declaration 只恢复 signature 允许省略的外围 `@escaping`/`@autoclosure`；具体泛型 owner 会按已冻结 specialization 替换和探测，不会加入有歧义的未 specialization alias。MainActor 声明中的非 Sendable callback 保留继承的 actor 限制，显式 `@Sendable` callback 保留自身 executor 合同。这条通用路径覆盖 Swift 与 Objective-C 声明、Swift overlay/物理 alias、SDK actor 隔离，以及 Objective-C 实例方法被导入为逻辑 Swift `throws -> Void` 时已经证明的 canonical `NSError **` bridge；例如 `UIColor.black`、`UIColor.init(white:alpha:)`、`UIViewController()`、`UIView.alpha`、`UIView.setNeedsLayout()`、`UIView.setAnimationsEnabled(_:)`、`UIView.performWithoutAnimation(_:)`、`UIView.animate(withDuration:animations:completion:)`、`UIButton.configurationUpdateHandler`、具体 X/Y `NSLayoutAnchor` member、`URLCache.shared`、`Bundle.path(forResource:ofType:)` 与 `FileManager.removeItem(atPath:)`。每个已冻结 imported SDK type 还会提名一次精确的无参 `Type()` 调用，即使继承或 importer 合成的 initializer 没有出现在 symbol graph；只有 frontend 证明该调用后才会纳入。其他继承型隐式 constructor、项目子类 constructor 与开放式/未 specialization 泛型 owner 不会扩张源码边界。已证明的 Objective-C protocol 输入仍冻结为 `AnyObject` ABI，但生成 invoker 会在需要时进入 MainActor 后按精确 existential 类型解码。本阶段的 suspending NativeImport 来自精确的项目源码发现或显式 catalog；受管 SDK 路径不会推断 async 声明或 completion-handler 转换。生产 Shell 不会得到这组便利能力，它本身也不会引入新的 boundary type。
- symbol graph 的 `T!` 隐式解包 Optional 会保留为合法 frontend 探针，并测成精确 Optional ABI。
- 精确原生操作已冻结时的 Objective-C superclass dispatch 与 address-form Optional 控制流。同类型 receiver cast 只有在两端是同一 reference `TypeID` 时才作为 alias；Optional payload take 即使经过精确地址复制，也必须受 `.some` edge 支配。
- 调用同 image helper、eligible Shell entry 与目标 Shell 已经生成的精确 allowlisted NativeImport。发布基线已经使用、且 Typed AST 语义与 canonical SIL 物理 ABI 能够对齐的外部 API 可以自动冻结；当前覆盖 reference、raw enum、OptionSet、opaque copyable value、accessor、method、全局值/函数与 operator、简单 imported C value、Selector、upcast、Foundation value-overlay bridge、Objective-C protocol 擦除、Swift `Any -> AnyObject` boxing，以及已验证的 String/Array Objective-C bridge。

- NativeImport callback 的通用发现与生成路径还覆盖带 closure 的原生 initializer、completion 参数和 callback 属性 setter，例如 `UIAction`/`UIAlertAction`、`UIViewController.present`、cell configuration handler 与 `Operation.completionBlock`。属性赋值是存储行为，因此合同会将其 closure 生命周期固定为 escaping，并保留表达式上的 actor isolation；这不是 UIKit 或 Foundation 的逐 API 特例。

- 上述泛型证明对用户 conformance 以精确完整的 frontend witness record 为准；已表示的标准值族只使用经过当前 toolchain 校验、且 VM 已有语义的集合、相等/比较、数值、`Strideable`、字面量、description 与无损解析闭合证据。受支持的精确 witness 包括比较、加减乘与原地修改、magnitude、整数除法/余数/位运算/独立整数 RHS 的移位、定宽整数边界与位属性、multiple/quotient 查询、wrapping/reporting-overflow 算术与 full-width 乘法、浮点除法/余数、distance/advance，以及 Bool、整数、浮点、String、扩展字形簇和 Unicode scalar 字面量。Compiler 会具体求解字面量 associated type 及 `Magnitude`/`Stride`/`Exponent` identity，协议操作也可复用普通 image-local closure 值。递归 `Hashable` 证据仅用于约束证明，不会伪造直接 Swift `Hasher` 执行；imported conformer 必须走具体已冻结 NativeImport，也不能从相似 storage 推断 conformance。

### 同步 closure 能力矩阵

下表是经过总审查的 v1 闭包基线。原生相关行仍要求存在精确冻结或由受管 Debug 生成的 NativeImport，并通过上文全部 callable 合同检查；示例不是 API allowlist。

| 领域 | 已支持 | 有意保留的边界 |
| --- | --- | --- |
| 构造与函数引用 | closure literal 与简写参数、局部/全局函数、operator/overload、补丁内绑定/未绑定 method（包括已证明的闭合具体条件 witness 和不可变闭合 existential protocol witness）、enum/Optional/Result case、补丁内 initializer/static factory、eligible Shell entry，以及表示保持的 NativeImport 全局/自由函数、绑定实例方法和 initializer | 仅适用于直接调用的默认参数投影不能伪装成函数值；未解析或无法证明的条件式、开放式、mutable-existential 或歧义 witness dispatch 会拒绝 |
| 存储与高阶传递 | Optional、Tuple、Array、Dictionary value、具体 Result、补丁内 struct/enum/class 字段、可变 closure 变量以及 closure 参数/结果；支持嵌套、递归和返回 closure | closure 不进入 Set key/element、VM-owned `Any`、Shell entry 或通用原生边界 codec |
| 捕获与所有权 | 不可变快照、共享 mutable cell、强捕获、安全 `weak`、checked `unowned`、捕获另一个 closure、可复制 imported owner，以及经过验证的词法期 nonescaping closure 对调用者 `inout` 的借用 | `unowned(unsafe)`、noncopyable capture、线性 `inout` capture，以及 escaping closure 捕获调用者 `inout` 会拒绝 |
| 调用与错误 | 同步 nonthrowing/throwing、具体 typed throws、具体 rethrows specialization、inout 的 normal/error 清理、autoclosure、默认参数 generator、Optional 调用、`callAsFunction`，以及完全具体的顺序 async/async-throws 调用 | async closure value/ABI、任意运行时 specialization 与通用 unwind cleanup 尚未实现 |
| 编译器管理的作用域 | 仅直接调用的 `defer`、动态检查的 `withoutActuallyEscaping`，以及类型通用的同步 `withExtendedLifetime`，包括 throwing 与 closure-valued result | `autoreleasepool`、`withUnsafe...` 和 contiguous-storage scope 必须保留真实原生运行时或 pointer lifetime 语义，不做近似 |
| 原生 callback 参数 | 直接或 Optional 的 Swift closure/Objective-C block；nonescaping/escaping 生命周期；同步 nonthrowing 参数与结果；MainActor provenance；经过检查的默认值；有界 `Error` 参数代理；确定性失败值 | throwing/async/`inout` callback ABI、C function pointer/context-pointer pair、递归或 nonescaping 的嵌套 callable、callable 容器，以及没有确定性失败值的结果 |
| 原生来源 callable | 一层直接或 Optional 的 escaping callable 可作为外层 callback 参数或 NativeImport 结果进入 VM，并以同一强类型 closure 路径保留 identity、ownership、actor、deadline 与资源检查 | 第二层 callable、callable aggregate，以及用 image-local closure 冒充原生返回值会拒绝 |
| Async NativeImport | 生成式精确全局/静态/实例函数与 getter，显式 catalog initializer；nonisolated 或 MainActor；普通 throwing/nonthrowing；连续 suspending deadline | 自动发现 async initializer、closure 参数/结果、callback、setter、generic/indirect ABI、completion-handler 转换、custom actor 与 async callable value 会拒绝 |
| 常见 SDK 场景 | UIKit animation/transition/action/presentation/configuration、`DispatchQueue.async`/`asyncAfter`、`DispatchGroup.notify`、Operation/OperationQueue、Timer、URLSession、NotificationCenter、`NSPredicate` 与 FileManager enumeration | 超出精确 profile 的泛型或 throwing SDK closure 声明（包括泛型 `DispatchQueue.sync` closure overload）在具备擦除表示的生成 wrapper 前仍要求正常构建 |
| 隔离与并发 | 同步 `@MainActor` closure/callback、同线程重入、escaping callback 保留原 image/generation lease；callback 执行串行化 | 不声明通用 `Sendable` 语义，不允许跨线程重叠 callback、custom global actor、actor-isolated `self` 或任意 executor hop |

### 拒绝或有意未完成

- generic root，以及仍需要运行时 generic metadata、运行时 witness table、未解析/泛型 reabstraction 或动态 specialization 的执行。完全具体且 requirement 已证明的条件 witness call，以及上文不可变的闭合 existential witness call，都会在 Compiler 中解析成 image call，不放宽这条运行时边界。这也包括尚未归一为可表示 managed Collection、迭代语义不透明的自定义 `Sequence`；Helix 不会把它们经 NativeImport 转交给 Swift 标准库执行。
- 隐式携带外层泛型 archetype 的嵌套 nominal、无法从精确 entry result buffer 证明 underlying type 的 opaque result、direct/indirect 混合的物理多结果 ABI，以及带多个间接 normal result 的 throwing call。这些都是显式编译期边界；Helix 不会生成运行时 opaque 或泛型 metadata 去猜测 layout。
- Task/Task.detached、async let、TaskGroup 等并行或非结构化执行，公开 continuation、async closure value、动态 existential async dispatch、AsyncSequence/AsyncStream 与通用 `Sendable` 语义仍不支持；取消只在上述顺序 VM/native checkpoint 协作观察。
- 跨 suspension 存活的 `inout`、address/access/borrow storage，actor-isolated instance root、custom global actor 与任意 executor hop 仍拒绝；完全具体 image function 之间精确的 nonisolated/MainActor 切换是有界例外。
- closure 不能穿过 Shell Entry，也不能在上述精确 callable profile 之外穿过 NativeImport。普通 native value 与通用 boundary codec 都不能包含 closure；只有精确 callback 参数，或直接/Optional 的原生 callable 结果，可以使用强类型 handle 边界。没有 framework-neutral 失败值的 callback 结果、throwing/async callback ABI、`inout` callback 参数、递归或 nonescaping 的嵌套 callable、callable 容器以及并发 `Sendable` 执行语义仍不支持；`unowned(unsafe)` 会因无法安全表达悬空引用而被拒绝，weak/unowned stored property 也尚未进入补丁内 nominal layout。调用者拥有的 `inout` 只能走上面经过验证的词法期 nonescaping 借用，escaping capture 仍会 fail closed。
- `String.Index`、基于 String index 的下标或修改、UTF-8/UTF-16/Unicode-scalar view、locale-sensitive 或 Foundation 文本 API，以及上文未列出的 Character 属性。它们会 fail closed，不会用整数 offset 或泛型 NativeImport 近似。超出上述定宽整数和浮点迭代面的 progression element type、把 Range/stride 值导出到 Shell 或 NativeImport 边界，以及函数局部 nominal type 声明也仍被拒绝。把不导出的补丁内 struct/enum 移到现有受监视文件的文件/module scope 后即可随补丁编译；只要它仍是 image 私有声明，就不要求重建 Shell。
- Dictionary key 或 Set element 的用户自定义 `Hashable` 语义仍会被拒绝。VM-owned `Any` 的闭合语法只允许递归且由 VM 定义 Hashable 语义的 key/element，因此不会隐式执行用户 witness。Swift Shell codec 会递归物化受支持的标量、文本、Optional、Array、Dictionary 与 Set，但仍拒绝 ArraySlice（类型擦除边界无法重建其非零公开索引基址）、Tuple、补丁内值、native object 与 closure；`Void` 也不能擦除，因为 HLVM 用“没有值”表示它。这些边界拒绝不会削弱同一 verified image 内 ArraySlice、Tuple 或补丁内值的内部精确身份。
- 任意新 Swift metadata、原生侧可识别的补丁具体 class、retroactive conformance，以及修改 Shell 已有类型的 layout、superclass 或 enum case。上面的 hosted Objective-C subclass 是冻结 superclass projection，不是动态生成任意 Swift metadata。
- Shell Entry 或普通 NativeImport 边界中的 Swift protocol existential value、进入 existential dispatch 集合的条件式或 imported conformer，以及 mutable existential opening/writeback。受支持的不可变 profile 只在当前 module 内完整的非条件 image-local conformer 上闭合；若穿过这些边界，就无法在不引入 Swift runtime metadata 的前提下安全扩展。另行证明的 Objective-C `!foreign` 擦除是冻结的原生 `AnyObject` value，并没有导出这份 image-local 表示。
- Generic Shell entry；noncopyable、递归、imported/native-backed 或依赖运行时 metadata 的 value root；typed-throws root；上述具体标准库操作之外的通用 `rethrows`；以及通用 unwind cleanup。Eligible 的同步 Shell 值变异只允许恰好一个逻辑 `inout` 区域，包括可变 `self`；多个或 async `inout`、显式 `_read`/`_modify`、async 或 typed-throws accessor、带 availability 的 function/accessor 声明或上下文、泛型 accessor 声明或泛型声明上下文、无法由生成代码命名 private 嵌套 receiver 的 accessor 与 mutable existential opening 仍会 fail closed。受支持的单区域会精确写回嵌套 projection 与 enum 状态，VM trap 不暴露任何 writeback。冻结 stored property 不能包含 closure、protocol existential、`AnyObject`、native value、生成的文件作用域代码无法命名的 private 嵌套 nominal，或其他不支持的值。带声明初始化器的 `let` 无法由安全的同源重建 initializer 再次赋值，因此拒绝；`var` 的声明初始化器可以存在，冻结 payload 仍是最终权威值。带 availability 属性的 value 声明或 enum case 也会拒绝，因为无条件 codec 无法在 Shell 的完整 deployment range 内合法引用它。
- 带额外原生运行时语义的 closure scope 仍不支持：`autoreleasepool` 必须建立真实的 autorelease-pool 边界，`withUnsafe...` 与 contiguous-storage callback 会暴露 pointer lifetime。它们不会被近似成 `withExtendedLifetime`，也不会被当作空操作 closure call。
- 不受限 pointer、`unsafeBitCast`、任意 Objective-C selector/IMP、`dlopen`/`dlsym`、Mirror 字段修改与未知 builtin。
- 目标 Shell 中没有精确 `NativeImportID` 的原生调用，即使 App 中存在名字相似的 Swift 函数。生产补丁也不能给旧 Shell 新增 framework，或首次使用发布时未冻结的 SDK 操作。上面的受管 Debug 调色板之所以可用，正是因为正常 Debug Build 已经逐项冻结了这些 ID。

## 开发期 Live Reload 边界

默认 Live Reload 使用与生产相同的 canonical SIL、Verifier 与 HLVM 核心。开发路径改变的是 session、传输、生命周期与诊断策略，不会用下载的机器码兜底不受支持的字节码。

| 修改 | 当前结果 |
| --- | --- |
| 修改已索引的 global 函数体 | canonical SIL 位于文档子集内时支持，包括完全具体的顺序 async/async-throws root |
| 修改已索引的源码 class 实例方法体 | 支持；生成 TypeOps 会把精确 `self` 引用传入 HLVM，nonisolated/MainActor async method 使用永久源码 body Bridge |
| 修改 eligible Shell 已有 struct/enum 的实例方法 | 支持 nonmutating 的普通、`borrowing`、`consuming` receiver；同步 `mutating` receiver 可作为该 Entry 唯一的逻辑 `inout` 区域。覆盖 extension method、嵌套/COW 修改、enum 状态转换，以及 normal/已声明 error 的精确写回；VM trap 不写回 |
| 修改已有计算属性或下标 | receiver、参数、结果、effect 与 body 均可表示时，支持精确同步 getter/setter root。覆盖 `mutating get`、`nonmutating set`、static/class/global 形式、源码 extension、accessor 独立 access control，以及 normal/已声明 error 值写回。显式 `_read`/`_modify`、async、typed throws、带 availability 或泛型的声明/上下文、无法由生成代码命名的 private 嵌套 receiver 与递归 Native accessor replacement 仍 fail closed |
| 修改已有 stored property 的 `willSet` 或 `didSet` body | 直接声明的同步 global、eligible frozen struct 及源码 reference-class observer 可通过 derived source 中绑定精确 hash 的原位 wrapper 独立补丁。保留隐式/自定义 old/new-value 名、baseline fallback、同文件 private 访问、value storage 直接修改及 value receiver 的事务性写回。static/class、继承、lazy/wrapped、weak/unowned/Objective-C、availability/泛型、actor/global-actor、baseline magic literal、old/new-value ABI shape 变化，以及 reference observer 对自身被观察属性的直接赋值会 fail closed；不会生成 observer Native replacement |
| 使用显式 `inout`，变异 actor root，或修改已有原生 static/class 方法 | 支持一个同步且 eligible 的 Shell `inout` 参数；多个/async 区域仍拒绝，actor executor 与 native metatype ABI 尚未实现 |
| 从函数体调用已有 private/internal/public 声明 | 仅在解析为同 image 函数、eligible Shell Entry 或实际生成的精确 NativeImport 时支持 |
| 在受管 Debug body 中首次使用公开 SDK 成员 | 对唯一测得、未 deprecated 的同步 initializer、实例/静态 method 或可读/可写 property 支持，前提是所有 boundary type 已能由冻结 imported/Bridge 类型面表达，且声明满足 Shell minimum OS；这包括对已冻结 imported SDK type 单独由编译器证明的无参 `Type()` 构造及具体 SDK 泛型 specialization 的 member，其他继承型隐式 constructor、项目子类 constructor 与开放式/未 specialization 泛型 owner 不会扩张源码边界。带 closure 的 member 必须符合上述同步、nonthrowing、bridge-and-failure-value profile，并保留声明级 `@escaping`/`@autoclosure` 与继承 MainActor 限制。async SDK 声明、completion-handler 转换、陌生 error bridge、subscript、不受支持的 actor hop 与不可表达签名需要完整构建；suspending NativeImport 当前来自精确的项目源码发现或显式 catalog |
| 在现有源码文件新增普通顶层 helper、class private 实例方法或计算 accessor | 能从变化 root 到达、且具体签名与函数体落在 HLBC 子集内时支持；声明仅属于该 image |
| 普通直接递归 | 解析到同一不可变 HLBC image 内的函数 |
| 从源码有意调用上一代 | HLBC 不支持；应保存/激活一个恢复 generation |
| 使用受支持的局部 closure，或调用已经索引且带 `@escaping` closure 参数的同 image helper | 降入同一 image；closure 的返回和捕获只能发生在固定的 VM invocation 内 |
| 把完全静态只读 KeyPath 字面量用作 transform 或直接投影 | patch-local struct/class stored field、具体 getter 链（包括 generated accessor 能解析到精确 NativeImport 的 imported Objective-C property）以及静态 Optional chain/force/wrap component 可以组合成强类型零捕获函数；动态 KeyPath value、subscript index 等带捕获 component、无法证明的 component，以及 writable/reference-writable mutation 会被拒绝，因为 KeyPath 对象不是 HLBC Runtime value |
| 使用整数 `Range`/`ClosedRange` 迭代、数值 `stride` 或可表示 Range 查询 | `contains`、`overlaps`、`clamped(to:)`、bound projection 与上述具体迭代族会保留已验证的边界、空区间、signed-zero、方向、开闭端点、零步长 trap 与整数极值语义。progression 值仍只属于 image，不能穿过 Shell/NativeImport 边界 |
| 在受支持文本/Sequence API 中使用 `String`、`Character` 或 `Substring` | 通过经过验证的 grapheme 与归一 Character 序列表示支持，也可穿过 Shell Bridge 往返；`String.Index`、index-sensitive 修改、UTF view 与未列出的 Character/Foundation API 仍被拒绝 |
| 声明补丁内 struct 或 enum | 新增的不导出类型在文件/module scope 支持，包括 namespace 嵌套和受支持的计算 accessor；函数局部 nominal 会用精确类型诊断拒绝 |
| 声明补丁内 pure class | `final`、非泛型、只在同一 image 内使用时支持引用 identity、stored property、private/普通方法与计算 accessor；不能传给原生代码 |
| 声明继承现有项目类或系统类的 hosted class | superclass 必须已作为 `NSObject` 兼容 reference TypeOps 冻结；当前支持继承无参初始化、无新增 stored property及无参/Bool `Void` override，并以 superclass 身份交给 UIKit/原生 API |
| 新增无关声明、新原生 ABI 表面或新 Swift 文件 | 不会仅因声明存在而收集；source membership 或原生 ABI 变化需要完整构建 |
| 修改 stored property、签名、generic constraint、actor isolation、superclass、conformance 或 enum case | 拒绝，需要完整构建 |
| 修改 default argument 行为 | 完全具体的 generator 会与同一完整 module 内 eligible、已归档的调用点一起进入补丁；跨 module public/package 默认值、非 eligible 调用点或泛型 ABI 要求完整构建 |
| 修改 static/global initializer | 已经初始化的状态不会自动重放 |
| 新增 framework、package、macro/plugin 输入、bridging header 或 source membership | Dev Build Manifest 失效，需要完整构建 |
| 修改 storyboard、XIB、asset、strings、Core Data model、plist 或 entitlement | 不属于 Swift body Live Reload 路径 |

捕获的编译器上下文会保留原 access control，但可见性本身不是 Runtime capability。一个操作无法表示成 HLBC、也没有精确生成的 Entry/NativeImport 时，即便普通 Swift 允许访问，编译仍会失败。

Simulator 与设备使用同一套 HLBC 协议和 Runtime。仓库 Simulator E2E 已在同一 App 进程中连续应用八代：修改/恢复 layout body、经 target-action 创建视图和 Auto Layout、escaping configuration handler、nonescaping/escaping 动画 callback、controller present/dismiss completion，以及最终源码恢复。测试会断言持久化 UIKit/App 状态并保留截图，不会把编译或激活本身当成成功；另有 128 代进程内 soak 验证 active/rollback、失败保存、generation 高水位与压缩保持有界。真实 iPhone 运行和长时间内存压力 soak 仍需补齐，才能列为经过验证。Native Dynamic Replacement 只保留为必须显式选择的内部实验，不是产品 fallback。

## UI 刷新边界

代码替换决定下一次函数调用的行为；UI invalidation 决定用户是否立即看到结果。

- Helix 会从已展示实例的运行时 class 自动还原 controller/view identity，并支持沿 superclass 匹配；UIKit 不需要 type registry。
- 常见 render 与 layout callback 会自动推导 constraint、layout 和 display invalidation，并保留原页面实例与内存状态。
- 初始化或业务自有刷新逻辑可以使用幂等 `LiveReload.Reloadable` hook；它不是普通接入前置条件。
- 重建 Controller 需要注册 Factory、route context、state capture/restore 和容器支持。
- SwiftUI 需要 `liveReloadBoundary`；`invalidateBody` 尽量保留 identity，`recreateSubtree` 会重置该 boundary 的局部状态。
- Helix 不会自动重放 `viewDidLoad`、`loadView`、initializer、observer 注册、subscription 或任意生命周期 callback。
- 没有安全目标或规则时，代码可以保持激活，但结果会是 `manualRefreshRequired`。

仓库 fixture 已证明无需注册的 UIKit controller/view 与 superclass 自动匹配、invalidation、状态保持、SwiftUI pulse 路由和 Debug Overlay 行为；它们不能认证所有 custom container、navigation/sheet 交互、observation graph 或长期副作用模式。

## 诊断边界

Canonical Swift debug metadata 会降低成经过 Verifier 检查的 HLBC source map，以 function、block 与 instruction offset 定位。生产 artifact 只保留无歧义的逻辑源码路径，不携带构建机绝对路径。反汇编可以标注这些位置；HLVM trap 会给出结构化 program counter，`HelixRuntime` 再补充固定的 generation、Shell entry、函数名与逻辑 Swift 位置后通知 observer。

这属于源码级故障归因，不等于替代 LLDB。HLBC 的交互式 breakpoint、单步、表达式求值与 time-travel debugging 尚未实现。不支持的语法仍会在 Mac 编译阶段用原逻辑源码位置明确失败。

## 安全与资源边界

生产与开发路径都会对未知版本、capability、目标、身份、重复记录、畸形容器和资源超限 fail closed。生产字节码具有 fuel、deadline、stack、register、调用深度、值形状、NativeImport 和内存计量。可变大小 VM 操作会原子预留已验证的最坏分配上界、退回未使用部分并保留实际计费；下载与 live transfer 会在分配和执行前进行有界检查。

开发期 Live Reload 会限制 artifact 字节与保留 generation。同步 Swift NativeImport 无法被硬抢占；只有具备 deadline 与 checkpoint 的 bounded/cooperative import 才适合进入生产 Catalog。真实设备尾延迟和内存压力仍是 Gate。

## 兼容性与分发

Swift Package 的平台下限为 macOS 14 与 iOS 15。补丁绑定一个 finalized Shell interface 与目标身份，不得猜测某个 App build 的包与另一个 build 兼容。

当前 Release Builder 允许 internal 与 enterprise HLBC policy。App Store 通道仍是 `policyBlocked`，受控 Native Release 后端尚未实现。平台政策、签名与组织审批独立于字节码引擎是否在技术上能运行。

完整流程见[总体架构](Architecture.zh-CN.md)、[生产热补丁](Production-Hot-Patching.zh-CN.md)和[开发期热重载](Development-Live-Reload.zh-CN.md)。
